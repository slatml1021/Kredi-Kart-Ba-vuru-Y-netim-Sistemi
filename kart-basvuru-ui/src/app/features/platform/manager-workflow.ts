import { CurrencyPipe, NgFor } from '@angular/common';
import { Component, computed, signal } from '@angular/core';
import { RouterLink } from '@angular/router';
import { PlatformApiService, WorkflowApplication, WorkflowOfficer, WorkflowOverview } from '../../core/services/platform-api.service';

@Component({
  selector: 'app-manager-workflow', imports: [RouterLink, CurrencyPipe, NgFor],
  template: `
    <style>.officer-title{display:flex;align-items:center;justify-content:space-between;margin-bottom:14px}.officer-card{display:grid;grid-template-columns:auto 1fr auto;gap:10px;align-items:center;border:1px solid #e2e7ed;border-radius:12px;padding:14px;text-align:left;cursor:pointer}.officer-card:hover,.officer-card.selected{border-color:#b88b25;background:#fff9e9;box-shadow:0 0 0 2px #b88b2525}</style>
    <section class="page"><header><div><small>OPERASYON VE YETKİ MERKEZİ</small><h1>İş Dağılımı ve Onaylar</h1><p>Başvuru yükünü personele dağıtın; kritik ve ikinci onay bekleyen kayıtları izleyin.</p></div><button (click)="load(true)" [disabled]="loading()">{{ loading() ? 'Yenileniyor...' : 'Yenile' }}</button></header>
    @if (message()) { <div class="message" [class.error]="hasError()">{{ message() }}</div> }
    <div class="metrics"><article><span>Açık Başvuru</span><b>{{ data()?.applications?.length || 0 }}</b></article><article><span>Atanmamış</span><b>{{ data()?.unassignedCount || 0 }}</b></article><article class="dual"><span>İkinci Onay</span><b>{{ data()?.secondApprovalCount || 0 }}</b></article><article class="late"><span>Geciken</span><b>{{ data()?.overdueCount || 0 }}</b></article></div>
    <div class="officers"><div class="officer-title"><div><h2>Memur İş Yükü</h2><p>Bir memura tıklayarak yalnızca ona atanmış işleri görüntüleyin.</p></div>@if(selectedOfficerId()){<button type="button" (click)="selectedOfficerId.set(null)">Filtreyi Temizle</button>}</div><div class="officer-grid">@for(o of data()?.officers || [];track o.userId){<button type="button" class="officer-card" [class.selected]="selectedOfficerId()===o.userId" (click)="toggleOfficer(o.userId)"><div class="avatar">{{ initials(o.fullName) }}</div><div><b>{{ o.fullName }}</b><small>{{ o.registrationNumber }} · {{ o.branch }}</small></div><strong>{{ o.openWorkCount }}</strong><span>{{ o.revisionCount }} revizyon · {{ o.overdueCount }} geciken</span></button>}</div></div>
    <div class="panel"><div class="panel-head"><div><h2>Açık Başvurular</h2><p>Kritik kayıtlar önce, ardından bekleme süresi en yüksek kayıtlar gösterilir.</p></div><div class="head-filters"><input placeholder="Başvuru veya müşteri ara" [value]="query()" (input)="query.set($any($event.target).value)"><input placeholder="Yeniden atama gerekçesi" [value]="assignmentReason()" (input)="assignmentReason.set($any($event.target).value)"></div></div>
    <div class="table-wrap"><table><thead><tr><th>Öncelik</th><th>Başvuru / Müşteri</th><th>Kart / Limit</th><th>Aşama</th><th>Sorumlu Memur</th><th>SLA</th><th></th></tr></thead><tbody>
    <tr *ngFor="let item of filtered(); trackBy: trackApplication"><td><span class="badge" [class]="'badge '+item.priority.toLowerCase()">{{item.priority}}</span></td><td><b>{{item.applicationNumber}}</b><small>{{item.customer}} · {{item.score}}/100</small></td><td>{{item.cardType}}<small>{{item.requestedLimit|currency:'TRY':'symbol-narrow':'1.0-0':'tr-TR'}}</small></td><td><b>{{stage(item)}}</b>@if(item.firstApprover){<small>1. onay: {{item.firstApprover}}</small>}</td><td><select [value]="pendingOfficer(item)" (change)="selectOfficer(item,$event)" [disabled]="busyId()===item.id"><option value="">Atanmamış</option><option *ngFor="let officer of data()?.officers || []; trackBy: trackOfficer" [value]="officer.userId">{{officer.fullName}} ({{officer.openWorkCount}})</option></select><button type="button" class="auto" (click)="saveAssignment(item)" [disabled]="busyId()===item.id || !pendingOfficer(item)">Atamayı Kaydet</button><button type="button" class="auto" (click)="autoAssign(item)" [disabled]="busyId()===item.id">Otomatik Ata</button></td><td><b [class.late-text]="item.slaStatus==='Gecikmiş'">{{item.slaStatus}}</b><small>{{item.waitingHours}} saat</small></td><td><a [routerLink]="['/manager/applications',item.id]">İncele →</a></td></tr>
    @if(filtered().length===0){<tr><td colspan="7" class="empty">Açık başvuru bulunmuyor.</td></tr>}</tbody></table></div></div>
    <div class="panel completed-panel"><div class="panel-head"><div><h2>Sonuçlanmış Başvurular</h2><p>Son onay ve ret kararlarını, sorumlu personel ve işlem tarihiyle birlikte görüntüleyin.</p></div><span class="result-count">Son {{ completedFiltered().length }} kayıt</span></div>
    <div class="table-wrap"><table class="completed-table"><thead><tr><th>Başvuru / Müşteri</th><th>Kart / Limit</th><th>Karar</th><th>Sorumlu Memur</th><th>Sonuç Tarihi</th><th></th></tr></thead><tbody>
    <tr *ngFor="let item of completedFiltered(); trackBy: trackApplication"><td><b>{{item.applicationNumber}}</b><small>{{item.customer}}</small></td><td>{{item.cardType}}<small>{{item.requestedLimit|currency:'TRY':'symbol-narrow':'1.0-0':'tr-TR'}}</small></td><td><span [class]="'result '+item.status.toLowerCase()">{{resultLabel(item.status)}}</span></td><td>{{item.assignedOfficer || 'Atanmamış'}}</td><td>{{formatDate(item.evaluatedAtUtc || item.createdAtUtc)}}</td><td><a [routerLink]="['/manager/applications',item.id]">Detayı Gör →</a></td></tr>
    @if(completedFiltered().length===0){<tr><td colspan="6" class="empty">Filtreye uygun sonuçlanmış başvuru bulunmuyor.</td></tr>}</tbody></table></div></div>
    <aside class="policy"><b>Onay politikası</b><span>100.000 TL üzeri veya yüksek riskli başvurular iki farklı müdür tarafından onaylanır. İlk onayı veren müdür ikinci onayı veremez.</span></aside></section>`,
  styles:[`:host{display:block;color:#0b2849}.page{max-width:1550px;margin:auto;padding:36px}.page>header{display:flex;justify-content:space-between;align-items:end;gap:20px;margin-bottom:25px}header small{color:#a97b13;font-weight:800;letter-spacing:.19em}h1{font-size:42px;margin:8px 0}p{color:#758399;margin:0}button,input,select{border:1px solid #ccd6e2;background:#fff;border-radius:10px;padding:12px 14px}.metrics{display:grid;grid-template-columns:repeat(4,1fr);gap:15px}.metrics article{background:#fff;border:1px solid #dce3eb;border-radius:14px;padding:20px}.metrics span{color:#758399;font-weight:700}.metrics b{font-size:31px;display:block;margin-top:8px}.metrics .dual{border-top:4px solid #7661b6}.metrics .late{border-top:4px solid #c13c45}.officers,.panel{background:#fff;border:1px solid #dce3eb;border-radius:16px;margin-top:18px;padding:22px}.officers h2,.panel h2{margin:0 0 14px}.officer-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(270px,1fr));gap:12px}.officer-grid article{display:grid;grid-template-columns:auto 1fr auto;gap:10px;align-items:center;border:1px solid #e2e7ed;border-radius:12px;padding:14px}.avatar{width:42px;height:42px;border-radius:50%;background:#0b3764;color:#fff;display:grid;place-items:center;font-weight:800}.officer-grid small,.officer-grid span,td small{display:block;color:#7d899b;margin-top:4px}.officer-grid strong{font-size:25px}.officer-grid span{grid-column:2/4}.panel{padding:0;overflow:hidden}.panel-head{display:flex;justify-content:space-between;align-items:end;padding:20px 22px;border-bottom:1px solid #e7ecf2}.panel-head h2{margin-bottom:5px}.head-filters{display:flex;gap:8px}.panel-head input{width:230px}.table-wrap{overflow:auto}table{width:100%;border-collapse:collapse;min-width:1150px}.completed-table{min-width:900px}th{background:#f6f8fb;color:#6d7b8e;text-align:left;padding:13px 16px;font-size:12px}td{padding:16px;border-top:1px solid #edf0f4}td select{min-width:190px}.auto{margin-top:6px;padding:6px 9px;color:#fff;background:#174f7b;font-size:11px;font-weight:800}td a{font-weight:800;color:#0b3d71;text-decoration:none}.badge{padding:6px 10px;border-radius:999px;font-weight:800}.badge.kritik{background:#fde8ea;color:#a92830}.badge.yüksek{background:#fff2d4;color:#926813}.badge.normal{background:#e7f4eb;color:#19703c}.late-text{color:#b52f37}.result-count{color:#718093;font-size:12px;font-weight:800}.result{display:inline-flex;padding:6px 10px;border-radius:999px;font-weight:800}.result.approved{color:#18734d;background:#e1f5eb}.result.rejected{color:#a52b2b;background:#fde7e7}.policy{display:flex;gap:12px;background:#f8f3e5;border-left:4px solid #bd8c19;padding:16px 18px;margin-top:18px;border-radius:10px}.message{background:#e7f4eb;color:#176c3b;padding:13px;border-radius:10px;margin-bottom:15px}.message.error{background:#fde8ea;color:#a92830}.empty{text-align:center;padding:45px;color:#7d899b}@media(max-width:900px){.page{padding:20px}.metrics{grid-template-columns:repeat(2,1fr)}h1{font-size:34px}.panel-head{align-items:start;gap:15px;flex-direction:column}.head-filters{width:100%;flex-direction:column}.panel-head input{width:100%;box-sizing:border-box}}`]
})
export class ManagerWorkflow {
 protected readonly data=signal<WorkflowOverview|null>(null);protected readonly query=signal('');protected readonly assignmentReason=signal('');protected readonly message=signal('');protected readonly hasError=signal(false);protected readonly busyId=signal<number|null>(null);protected readonly selectedOfficerId=signal<number|null>(null);protected readonly loading=signal(false);private readonly pendingAssignments=signal<Record<number,number>>({});
 protected readonly filtered=computed(()=>{const q=this.query().trim().toLocaleLowerCase('tr-TR');const officer=this.selectedOfficerId();return (this.data()?.applications||[]).filter(x=>(!officer||x.assignedOfficerUserId===officer)&&(!q||`${x.applicationNumber} ${x.customer}`.toLocaleLowerCase('tr-TR').includes(q)))});
 protected readonly completedFiltered=computed(()=>{const q=this.query().trim().toLocaleLowerCase('tr-TR');const officer=this.selectedOfficerId();return (this.data()?.completedApplications||[]).filter(x=>(!officer||x.assignedOfficerUserId===officer)&&(!q||`${x.applicationNumber} ${x.customer}`.toLocaleLowerCase('tr-TR').includes(q)))});
 constructor(private readonly api:PlatformApiService){this.load()}
 protected load(showMessage=false){this.loading.set(true);this.api.workflowOverview().subscribe({next:x=>{this.data.set(x);this.pendingAssignments.set(Object.fromEntries(x.applications.map(item=>[item.id,item.assignedOfficerUserId??0])));this.loading.set(false);this.hasError.set(false);if(showMessage)this.message.set('İş dağılımı güncellendi.')},error:()=>{this.loading.set(false);this.hasError.set(true);this.message.set('İş dağılımı yüklenemedi.')}})}
 protected pendingOfficer(item:WorkflowApplication){return this.pendingAssignments()[item.id]??item.assignedOfficerUserId??0}
 protected selectOfficer(item:WorkflowApplication,e:Event){const id=Number((e.target as HTMLSelectElement).value);this.pendingAssignments.update(values=>({...values,[item.id]:id}));this.message.set(id?'Memur seçildi. Kaydetmek için “Atamayı Kaydet” düğmesine basın.':'Aktif bir memur seçin.');this.hasError.set(!id)}
 protected saveAssignment(item:WorkflowApplication){const id=this.pendingOfficer(item);if(!id){this.message.set('Atamayı kaldırmak yerine başka bir aktif memur seçin.');this.hasError.set(true);return}const isReassignment=!!item.assignedOfficerUserId&&item.assignedOfficerUserId!==id;const reason=this.assignmentReason().trim();if(isReassignment&&reason.length<10){this.message.set('Yeniden atama için en az 10 karakterlik gerekçe girin.');this.hasError.set(true);return}this.busyId.set(item.id);this.api.assignApplication(item.id,id,reason||null).subscribe({next:()=>{this.message.set('Atama kalıcı olarak kaydedildi.');this.assignmentReason.set('');this.hasError.set(false);this.busyId.set(null);this.load()},error:r=>{this.message.set(r.error?.detail??'Atama kaydedilemedi.');this.hasError.set(true);this.busyId.set(null);this.pendingAssignments.update(values=>({...values,[item.id]:item.assignedOfficerUserId??0}))}})}
 protected autoAssign(item:WorkflowApplication){this.busyId.set(item.id);this.api.autoAssignApplication(item.id).subscribe({next:()=>{this.message.set('Şube ve iş yükü kurallarına göre otomatik atama yapıldı.');this.hasError.set(false);this.busyId.set(null);this.load()},error:r=>{this.message.set(r.error?.detail??'Otomatik atama yapılamadı.');this.hasError.set(true);this.busyId.set(null)}})}
 protected trackApplication(_index:number,item:WorkflowApplication){return item.id}
 protected trackOfficer(_index:number,item:WorkflowOfficer){return item.userId}
 protected toggleOfficer(id:number){this.selectedOfficerId.set(this.selectedOfficerId()===id?null:id)}
 protected initials(v:string){return v.split(' ').slice(0,2).map(x=>x[0]).join('').toUpperCase()} protected stage(x:WorkflowApplication){return x.workflowStage==='SecondManagerApproval'?'İkinci müdür onayı':x.workflowStage==='OfficerRevision'?'Memur revizyonu':'Müdür değerlendirmesi'}
 protected resultLabel(status:string){return status==='Approved'?'Onaylandı':status==='Rejected'?'Reddedildi':status}
 protected formatDate(value:string){return new Date(value).toLocaleString('tr-TR',{dateStyle:'short',timeStyle:'short'})}
}
